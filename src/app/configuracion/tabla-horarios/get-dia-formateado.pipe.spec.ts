import { GetDiaFormateadoPipe } from './get-dia-formateado.pipe';

describe('GetDiaFormateadoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDiaFormateadoPipe();
    expect(pipe).toBeTruthy();
  });
});

import { GetFechaFromServicioPipe } from './get-fecha-from-servicio.pipe';

describe('GetFechaFromServicioPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFechaFromServicioPipe();
    expect(pipe).toBeTruthy();
  });
});
